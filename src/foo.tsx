
import * as  React from "react"
import * as ReactDOM from "react-dom"


const Index = () => <h1>Hello React!</h1>;

export const go = () => {
    // ReactDOM.render(<Index />, document.getElementById('index'))
}